/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { JUKEBOXDAPP_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_DISCOGRAPHY_DISCRIMINATOR = new Uint8Array([
  204, 43, 46, 76, 21, 231, 189, 255,
]);

export function getUpdateDiscographyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_DISCOGRAPHY_DISCRIMINATOR
  );
}

export type UpdateDiscographyInstruction<
  TProgram extends string = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
  TAccountDiscography extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDiscography extends string
        ? WritableAccount<TAccountDiscography>
        : TAccountDiscography,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateDiscographyInstructionData = {
  discriminator: ReadonlyUint8Array;
  album: string;
  artist: string;
  songName: string;
  songUrl: string;
};

export type UpdateDiscographyInstructionDataArgs = {
  album: string;
  artist: string;
  songName: string;
  songUrl: string;
};

export function getUpdateDiscographyInstructionDataEncoder(): Encoder<UpdateDiscographyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['album', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['artist', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['songName', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['songUrl', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_DISCOGRAPHY_DISCRIMINATOR })
  );
}

export function getUpdateDiscographyInstructionDataDecoder(): Decoder<UpdateDiscographyInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['album', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['artist', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['songName', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['songUrl', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getUpdateDiscographyInstructionDataCodec(): Codec<
  UpdateDiscographyInstructionDataArgs,
  UpdateDiscographyInstructionData
> {
  return combineCodec(
    getUpdateDiscographyInstructionDataEncoder(),
    getUpdateDiscographyInstructionDataDecoder()
  );
}

export type UpdateDiscographyInput<
  TAccountDiscography extends string = string,
  TAccountAuthority extends string = string,
> = {
  discography: Address<TAccountDiscography>;
  authority: TransactionSigner<TAccountAuthority>;
  album: UpdateDiscographyInstructionDataArgs['album'];
  artist: UpdateDiscographyInstructionDataArgs['artist'];
  songName: UpdateDiscographyInstructionDataArgs['songName'];
  songUrl: UpdateDiscographyInstructionDataArgs['songUrl'];
};

export function getUpdateDiscographyInstruction<
  TAccountDiscography extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
>(
  input: UpdateDiscographyInput<TAccountDiscography, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): UpdateDiscographyInstruction<
  TProgramAddress,
  TAccountDiscography,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? JUKEBOXDAPP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    discography: { value: input.discography ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.discography),
      getAccountMeta(accounts.authority),
    ],
    data: getUpdateDiscographyInstructionDataEncoder().encode(
      args as UpdateDiscographyInstructionDataArgs
    ),
    programAddress,
  } as UpdateDiscographyInstruction<
    TProgramAddress,
    TAccountDiscography,
    TAccountAuthority
  >);
}

export type ParsedUpdateDiscographyInstruction<
  TProgram extends string = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    discography: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateDiscographyInstructionData;
};

export function parseUpdateDiscographyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateDiscographyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { discography: getNextAccount(), authority: getNextAccount() },
    data: getUpdateDiscographyInstructionDataDecoder().decode(instruction.data),
  };
}
