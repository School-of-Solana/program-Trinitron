/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { JUKEBOXDAPP_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DELETE_DISCOGRAPHY_DISCRIMINATOR = new Uint8Array([
  162, 211, 4, 135, 188, 249, 176, 108,
]);

export function getDeleteDiscographyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DELETE_DISCOGRAPHY_DISCRIMINATOR
  );
}

export type DeleteDiscographyInstruction<
  TProgram extends string = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDiscography extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDiscography extends string
        ? WritableAccount<TAccountDiscography>
        : TAccountDiscography,
      ...TRemainingAccounts,
    ]
  >;

export type DeleteDiscographyInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type DeleteDiscographyInstructionDataArgs = {};

export function getDeleteDiscographyInstructionDataEncoder(): FixedSizeEncoder<DeleteDiscographyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: DELETE_DISCOGRAPHY_DISCRIMINATOR })
  );
}

export function getDeleteDiscographyInstructionDataDecoder(): FixedSizeDecoder<DeleteDiscographyInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDeleteDiscographyInstructionDataCodec(): FixedSizeCodec<
  DeleteDiscographyInstructionDataArgs,
  DeleteDiscographyInstructionData
> {
  return combineCodec(
    getDeleteDiscographyInstructionDataEncoder(),
    getDeleteDiscographyInstructionDataDecoder()
  );
}

export type DeleteDiscographyInput<
  TAccountAuthority extends string = string,
  TAccountDiscography extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  discography: Address<TAccountDiscography>;
};

export function getDeleteDiscographyInstruction<
  TAccountAuthority extends string,
  TAccountDiscography extends string,
  TProgramAddress extends Address = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
>(
  input: DeleteDiscographyInput<TAccountAuthority, TAccountDiscography>,
  config?: { programAddress?: TProgramAddress }
): DeleteDiscographyInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDiscography
> {
  // Program address.
  const programAddress = config?.programAddress ?? JUKEBOXDAPP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    discography: { value: input.discography ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.discography),
    ],
    data: getDeleteDiscographyInstructionDataEncoder().encode({}),
    programAddress,
  } as DeleteDiscographyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDiscography
  >);
}

export type ParsedDeleteDiscographyInstruction<
  TProgram extends string = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    discography: TAccountMetas[1];
  };
  data: DeleteDiscographyInstructionData;
};

export function parseDeleteDiscographyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDeleteDiscographyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), discography: getNextAccount() },
    data: getDeleteDiscographyInstructionDataDecoder().decode(instruction.data),
  };
}
