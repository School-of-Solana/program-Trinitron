/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { JUKEBOXDAPP_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CREATE_DISCOGRAPHY_DISCRIMINATOR = new Uint8Array([
  58, 188, 116, 70, 160, 86, 190, 45,
]);

export function getCreateDiscographyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_DISCOGRAPHY_DISCRIMINATOR
  );
}

export type CreateDiscographyInstruction<
  TProgram extends string = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
  TAccountDiscography extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDiscography extends string
        ? WritableAccount<TAccountDiscography>
        : TAccountDiscography,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateDiscographyInstructionData = {
  discriminator: ReadonlyUint8Array;
  album: string;
  artist: string;
  songName: string;
  songUrl: string;
};

export type CreateDiscographyInstructionDataArgs = {
  album: string;
  artist: string;
  songName: string;
  songUrl: string;
};

export function getCreateDiscographyInstructionDataEncoder(): Encoder<CreateDiscographyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['album', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['artist', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['songName', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['songUrl', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_DISCOGRAPHY_DISCRIMINATOR })
  );
}

export function getCreateDiscographyInstructionDataDecoder(): Decoder<CreateDiscographyInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['album', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['artist', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['songName', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['songUrl', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCreateDiscographyInstructionDataCodec(): Codec<
  CreateDiscographyInstructionDataArgs,
  CreateDiscographyInstructionData
> {
  return combineCodec(
    getCreateDiscographyInstructionDataEncoder(),
    getCreateDiscographyInstructionDataDecoder()
  );
}

export type CreateDiscographyAsyncInput<
  TAccountDiscography extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  discography?: Address<TAccountDiscography>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  album: CreateDiscographyInstructionDataArgs['album'];
  artist: CreateDiscographyInstructionDataArgs['artist'];
  songName: CreateDiscographyInstructionDataArgs['songName'];
  songUrl: CreateDiscographyInstructionDataArgs['songUrl'];
};

export async function getCreateDiscographyInstructionAsync<
  TAccountDiscography extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
>(
  input: CreateDiscographyAsyncInput<
    TAccountDiscography,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateDiscographyInstruction<
    TProgramAddress,
    TAccountDiscography,
    TAccountPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? JUKEBOXDAPP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    discography: { value: input.discography ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.discography.value) {
    accounts.discography.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([100, 105, 115, 99, 111, 103, 114, 97, 112, 104, 121])
        ),
        getAddressEncoder().encode(expectAddress(accounts.payer.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.discography),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateDiscographyInstructionDataEncoder().encode(
      args as CreateDiscographyInstructionDataArgs
    ),
    programAddress,
  } as CreateDiscographyInstruction<
    TProgramAddress,
    TAccountDiscography,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type CreateDiscographyInput<
  TAccountDiscography extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  discography: Address<TAccountDiscography>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  album: CreateDiscographyInstructionDataArgs['album'];
  artist: CreateDiscographyInstructionDataArgs['artist'];
  songName: CreateDiscographyInstructionDataArgs['songName'];
  songUrl: CreateDiscographyInstructionDataArgs['songUrl'];
};

export function getCreateDiscographyInstruction<
  TAccountDiscography extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
>(
  input: CreateDiscographyInput<
    TAccountDiscography,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateDiscographyInstruction<
  TProgramAddress,
  TAccountDiscography,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? JUKEBOXDAPP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    discography: { value: input.discography ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.discography),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateDiscographyInstructionDataEncoder().encode(
      args as CreateDiscographyInstructionDataArgs
    ),
    programAddress,
  } as CreateDiscographyInstruction<
    TProgramAddress,
    TAccountDiscography,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedCreateDiscographyInstruction<
  TProgram extends string = typeof JUKEBOXDAPP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    discography: TAccountMetas[0];
    payer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateDiscographyInstructionData;
};

export function parseCreateDiscographyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateDiscographyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      discography: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateDiscographyInstructionDataDecoder().decode(instruction.data),
  };
}
