/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const DISCOGRAPHY_DISCRIMINATOR = new Uint8Array([
  8, 240, 174, 207, 157, 153, 115, 71,
]);

export function getDiscographyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DISCOGRAPHY_DISCRIMINATOR);
}

export type Discography = {
  discriminator: ReadonlyUint8Array;
  album: string;
  artist: string;
  songName: string;
  songUrl: string;
  authority: Address;
};

export type DiscographyArgs = {
  album: string;
  artist: string;
  songName: string;
  songUrl: string;
  authority: Address;
};

export function getDiscographyEncoder(): Encoder<DiscographyArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['album', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['artist', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['songName', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['songUrl', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['authority', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: DISCOGRAPHY_DISCRIMINATOR })
  );
}

export function getDiscographyDecoder(): Decoder<Discography> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['album', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['artist', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['songName', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['songUrl', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['authority', getAddressDecoder()],
  ]);
}

export function getDiscographyCodec(): Codec<DiscographyArgs, Discography> {
  return combineCodec(getDiscographyEncoder(), getDiscographyDecoder());
}

export function decodeDiscography<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Discography, TAddress>;
export function decodeDiscography<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Discography, TAddress>;
export function decodeDiscography<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Discography, TAddress> | MaybeAccount<Discography, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDiscographyDecoder()
  );
}

export async function fetchDiscography<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Discography, TAddress>> {
  const maybeAccount = await fetchMaybeDiscography(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDiscography<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Discography, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDiscography(maybeAccount);
}

export async function fetchAllDiscography(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Discography>[]> {
  const maybeAccounts = await fetchAllMaybeDiscography(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDiscography(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Discography>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDiscography(maybeAccount));
}
